1.github 二次上传
	1.$ git add .
	2.$ git commit -m "cs" cs为命名.
	3.$ git push -u origin master

#js笔记
####斜塔代码
    for(var a=0; a<5;a++){
        for(var b=0;b<a;b++){
             document.write("*")
        }
            document.write("</br>")
    }
    